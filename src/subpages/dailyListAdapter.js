import React from 'react'

const dailyListAdapter = () => {
    // const { TimeList } = props
    return (
        <> Hello</>
        // TimeList?.map((eachVal, i) => {
        //     return < TimeSlot TimeDetails={eachVal} key={eachVal.id} />
        // })
    )
}

export default dailyListAdapter